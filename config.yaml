# Example configuration file
app:
  name: "app-from-file"
  version: "1.0.0"
  environment: "development"

server:
  host: "localhost"
  port: 4000
  timeout: 30

database:
  host: "localhost"
  port: 5432
  username: "admin"
  password: "secret"
  name: "mydb"

logging:
  level: "info"
  format: "json"
